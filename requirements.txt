# PylaAi-XXZ — Python 3.10+ 64-bit, Windows only
#
# 1. pip install -r requirements.txt
#
# 2. PyTorch (CPU build, required by ultralytics / easyocr):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# 3. ONNX Runtime — install ONLY ONE of these:
#    Most Windows GPUs (NVIDIA / AMD / Intel):
#      pip install onnxruntime-directml==1.24.4
#    NVIDIA CUDA (only if CUDA + cuDNN are already installed):
#      pip install onnxruntime-gpu
#    CPU only:
#      pip install onnxruntime
#
# 4. scrcpy client (not on PyPI):
#    pip install https://github.com/leng-yue/py-scrcpy-client/archive/refs/tags/v0.5.0.zip --no-deps

numpy==1.26.4
opencv-python==4.8.0.76
adbutils==2.12.0
av==12.3.0
Pillow>=10.0.0
aiohttp
requests
packaging>=23.0
toml>=0.10.2
psutil>=7.0.0
websockets>=15.0
discord.py>=2.3.2
customtkinter>=5.2.0
PySide6>=6.7.0
pyautogui>=0.9.54
easyocr
ultralytics
google-play-scraper
